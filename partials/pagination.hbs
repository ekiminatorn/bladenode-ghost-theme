<nav class="pagination" role="navigation">
    <div id="pagination-wrapper">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><span class="glyphicon glyphicon-arrow-left"></span>&nbsp Newer Posts</a>
    {{/if}}
    <span class="page-number">Page {{page}} of {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Older Posts &nbsp<span class="glyphicon glyphicon-arrow-right"></span></a>
    {{/if}}
    </div>
</nav>